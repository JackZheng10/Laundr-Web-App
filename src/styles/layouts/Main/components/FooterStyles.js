const footerStyles = (theme) => ({
  footer: {
    marginTop: "auto",
    // backgroundColor: "red",
    backgroundColor: "#EEEEEE",
    // overflow: "hidden",
  },
  item: {
    padding: 20,
  },
});

export default footerStyles;
